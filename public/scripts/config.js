/**
 * Central frontend configuration.
 *
 * Keeping endpoints here prevents API paths and timing values from being
 * repeated throughout the user-interface code.
 */
export const CHAT_ENDPOINT = "/api/chat";

export const REQUEST_TIMEOUT_MS = 15_000;